# ── App ───────────────────────────────────────────────────────
PORT=3000
JWT_SECRET=change-this-to-a-long-random-secret-min-32-chars

# ── MySQL ─────────────────────────────────────────────────────
# For Docker: DB_HOST must be "db" (the docker-compose service name)
# For local dev: change DB_HOST=127.0.0.1
DB_HOST=db
DB_PORT=3306
DB_USER=mmotask_user
DB_PASSWORD=change-this-db-password
DB_NAME=mmotask_db

# Root password for the MySQL container (Docker only)
MYSQL_ROOT_PASSWORD=change-this-root-password
